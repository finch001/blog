## 概述
Rollup 是一个 JavaScript 模块打包器，可以将小块代码编译成大块复杂的代码，例如 library。Rollup默认对代码模块使用新的标准化格式，这些标准都包含在 JavaScript 的 ES6 版本中。ES6 模块最终还是要由浏览器原生实现，但当前 Rollup 可以使你提前体验。

### 为什么
将项目拆分为小的单独文件中，这样开发软件通常比较简单，以及显著降低了所要解决的问题的复杂度(complexity of the problem)。

### Tree-shaking
[Tree-shaking其实没什么卵用](./tree-shaking)
